---
description: Alias for /evidence_review — smartclaw PR green conditions check (pytest, CI, CodeRabbit, beads)
type: orchestration
execution_mode: immediate
---

# /er — Evidence Review Alias

Alias for `/evidence_review`. See that command for full documentation.

**Usage**: `/er [subject, PR number, or path]`

When this command is invoked with `$ARGUMENTS`, execute `/evidence_review $ARGUMENTS`.
