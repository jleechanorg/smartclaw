---
description: Alias for /coderabbit — Post "@coderabbitai all good?" on the current branch's PR (only after pushing fixes)
type: git
execution_mode: immediate
---
## ⚡ EXECUTION INSTRUCTIONS FOR CLAUDE
When this command is invoked, execute `/coderabbit` immediately.

This is an alias. Delegate all functionality to `/coderabbit`. Use the exact handle `@coderabbitai all good?` and only after a fix push.
